using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Controls the multiplier values for stacking effects
/// As an example a long sword sequence with an multiplier of 4 will have 4 times the desired properties 
/// </summary>
public class Multiplier : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //Pick first effect as main effect
    //
}
